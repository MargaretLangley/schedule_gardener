<nav class = 'navbar-default' role='navigation'>
  <div class = 'container'>  <!-- padding to keep away from edges -->
    <div class = 'navbar-header'>
      <!-- collapse responsive button -->
      <button type = 'button' class = 'navbar-toggle  collapsed' data-toggle = 'collapse' data-target = '.navbar-collapse'>
        <span class = 'sr-only'>Toggle navigation</span>
        <span class = 'icon-bar'></span>
        <span class = 'icon-bar'></span>
        <span class = 'icon-bar'></span>
      </button>
      <a id='logo' class = 'navbar-brand' href ='<%= root_path %>'>Garden Care <span class = 'glyphicon  glyphicon-leaf'></span></a>
    </div>

    <div class = 'collapse  navbar-collapse'>
      <ul class = 'nav  navbar-nav  navbar-right'>
        <% if masquerading? %>
          <%= link_to "Stop Masquerading", endmasquerade_path %>
        <% end %>
        <% if signed_in? %>
          <li id='fat-menu' class = 'dropdown'>
            <li class = 'dropdown'>
              <a href='#' class = 'dropdown-toggle' data-toggle='dropdown'><span class="glyphicon  glyphicon-user"></span> <%= current_user.full_name %><b class = 'caret'></b></a>
              <ul class = 'dropdown-menu'>
                <li><%= link_to 'Update Profile', edit_profile_path(current_user) %></li>
                <% if current_admin? %>
                  <li><%= link_to 'Administrate', rails_admin_path %></li>
                <% end %>
                <li class = 'divider'></li>
                <li>
                  <%= link_to 'Sign out', signout_path, method: 'delete' %>
                </li>
              </ul>
            </li>
          </li>
        <% else %>
          <li><%= link_to 'Sign in', signin_path %></li>
        <% end %>
      </ul>
    </div>  <!-- collapse navbar-collapse -->
  </div>  <!-- container -->
</nav>
