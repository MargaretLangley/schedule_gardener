<table class = "table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Phone</th>
      <th>Visit</th>
      <th>Contact From</th>
      <th>Phone No</th>
      <th>Edit</th>
      <th>Delete</th>
    </tr>
  </thead>
  <tbody>
    <% if object.count > 0 %>
      <% object.each do |touch| %>
        <tr>
          <td><%= touch.full_name %></td>
          <td><%= touch.by_phone? ? 'Yes' : 'No' %></td>
          <td><%= touch.by_visit? ? 'Yes' : 'No' %></td>
          <td><%= l touch.touch_from  %></td>
          <td><%= phone_without_area_code(touch.home_phone) %></td>
          <td><%= link_to_edit(edit_touch_path(touch)) %></td>
          <td><%= link_to_delete(touch) %></td>
        </tr>
      <% end %>
    <% else %>
      <tr><td colspan="7"><h3>Nobody to contact</h3></td></tr>
    <% end %>
  </tbody>
</table>